# Upload your two files here

1. **TaxiCalendar_....txt** — Outlook macro export  
2. **ABC_Invoice_....txt** — invoice saved as plain text  

Use GitHub **Add file → Upload files**, then commit.

Keep **only one** of each name pattern in this folder (delete old months before uploading new files).

Then run **Actions → Compare invoice vs calendar**.
